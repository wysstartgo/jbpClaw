diff --git a/extensions/deepseek/index.ts b/extensions/deepseek/index.ts
index 7f6b9a32c1..b2945ec5d4 100644
--- a/extensions/deepseek/index.ts
+++ b/extensions/deepseek/index.ts
@@ -1,9 +1,27 @@
 import { readConfiguredProviderCatalogEntries } from "openclaw/plugin-sdk/provider-catalog-shared";
+import { buildProviderReplayFamilyHooks } from "openclaw/plugin-sdk/provider-model-shared";
 import { defineSingleProviderPluginEntry } from "openclaw/plugin-sdk/provider-entry";
 import { applyDeepSeekConfig, DEEPSEEK_DEFAULT_MODEL_REF } from "./onboard.js";
 import { buildDeepSeekProvider } from "./provider-catalog.js";
 
 const PROVIDER_ID = "deepseek";
+
+const OPENAI_COMPATIBLE_REPLAY_HOOKS = buildProviderReplayFamilyHooks({
+  family: "openai-compatible",
+});
+const DEEPSEEK_REASONING_MODEL_IDS = new Set(["deepseek-reasoner", "deepseek-r1"]);
+const DEEPSEEK_V4_MODEL_PATTERN = /^deepseek-v4(?:[-_.]|$)/i;
+
+function isDeepSeekReasoningModel(modelId: unknown, reasoning: boolean | undefined): boolean {
+  if (reasoning) {
+    return true;
+  }
+  if (typeof modelId !== "string") {
+    return false;
+  }
+  const normalized = modelId.trim().toLowerCase();
+  return DEEPSEEK_REASONING_MODEL_IDS.has(normalized) || DEEPSEEK_V4_MODEL_PATTERN.test(normalized);
+}
 
 export default defineSingleProviderPluginEntry({
   id: PROVIDER_ID,
@@ -38,7 +56,10 @@ export default defineSingleProviderPluginEntry({
         config,
         providerId: PROVIDER_ID,
       }),
+    ...OPENAI_COMPATIBLE_REPLAY_HOOKS,
+    resolveDefaultThinkingLevel: ({ modelId, reasoning }) =>
+      isDeepSeekReasoningModel(modelId, reasoning) ? "medium" : "off",
     matchesContextOverflowError: ({ errorMessage }) =>
       /\bdeepseek\b.*(?:input.*too long|context.*exceed)/i.test(errorMessage),
   },
 });
diff --git a/extensions/xiaomi/index.ts b/extensions/xiaomi/index.ts
index b4166c642d..868dd0d2b 100644
--- a/extensions/xiaomi/index.ts
+++ b/extensions/xiaomi/index.ts
@@ -1,9 +1,15 @@
 import { defineSingleProviderPluginEntry } from "openclaw/plugin-sdk/provider-entry";
+import { buildProviderReplayFamilyHooks } from "openclaw/plugin-sdk/provider-model-shared";
 import { PROVIDER_LABELS } from "openclaw/plugin-sdk/provider-usage";
 import { applyXiaomiConfig, XIAOMI_DEFAULT_MODEL_REF } from "./onboard.js";
 import { buildXiaomiProvider } from "./provider-catalog.js";
 
 const PROVIDER_ID = "xiaomi";
+const XIAOMI_DEFAULT_THINKING_LEVEL = "medium";
+
+const OPENAI_COMPATIBLE_REPLAY_HOOKS = buildProviderReplayFamilyHooks({
+  family: "openai-compatible",
+});
 
 export default defineSingleProviderPluginEntry({
   id: PROVIDER_ID,
@@ -28,6 +34,9 @@ export default defineSingleProviderPluginEntry({
     catalog: {
       buildProvider: buildXiaomiProvider,
     },
+    ...OPENAI_COMPATIBLE_REPLAY_HOOKS,
+    resolveDefaultThinkingLevel: ({ reasoning }) =>
+      reasoning ? XIAOMI_DEFAULT_THINKING_LEVEL : "off",
     resolveUsageAuth: async (ctx) => {
       const apiKey = ctx.resolveApiKeyFromConfigAndStore({
         envDirect: [ctx.env.XIAOMI_API_KEY],
diff --git a/extensions/xiaomi/onboard.test.ts b/extensions/xiaomi/onboard.test.ts
index 615f1115e1..aa5f7dd1de 100644
--- a/extensions/xiaomi/onboard.test.ts
+++ b/extensions/xiaomi/onboard.test.ts
@@ -3,4 +3,6 @@ import {
   expectProviderOnboardMergedLegacyConfig,
   expectProviderOnboardPrimaryModel,
 } from "../../test/helpers/plugins/provider-onboard.js";
+import { registerSingleProviderPlugin } from "../../test/helpers/plugins/plugin-registration.js";
+import xiaomiPlugin from "./index.js";
 import { applyXiaomiConfig, applyXiaomiProviderConfig } from "./onboard.js";
@@ -19,6 +21,24 @@ describe("xiaomi onboard", () => {
     });
   });
-
+  it("defaults reasoning models to MiMo thinking mode", async () => {
+    const provider = await registerSingleProviderPlugin(xiaomiPlugin);
+
+    expect(
+      provider.resolveDefaultThinkingLevel?.({
+        provider: "xiaomi",
+        modelId: "mimo-v2-flash",
+        reasoning: true,
+      }),
+    ).toBe("medium");
+    expect(
+      provider.resolveDefaultThinkingLevel?.({
+        provider: "xiaomi",
+        modelId: "custom-chat-model",
+        reasoning: false,
+      }),
+    ).toBe("off");
+  });
+
   it("merges Xiaomi models and keeps existing provider overrides", () => {
     const provider = expectProviderOnboardMergedLegacyConfig({
       applyProviderConfig: applyXiaomiProviderConfig,
diff --git a/extensions/xiaomi/provider-catalog.ts b/extensions/xiaomi/provider-catalog.ts
index 92941caea7..2ff873b55a 100644
--- a/extensions/xiaomi/provider-catalog.ts
+++ b/extensions/xiaomi/provider-catalog.ts
@@ -19,7 +19,7 @@ export function buildXiaomiProvider(): ModelProviderConfig {
       {
         id: XIAOMI_DEFAULT_MODEL_ID,
         name: "Xiaomi MiMo V2 Flash",
-        reasoning: false,
+        reasoning: true,
         input: ["text"],
         cost: XIAOMI_DEFAULT_COST,
         contextWindow: XIAOMI_DEFAULT_CONTEXT_WINDOW,
