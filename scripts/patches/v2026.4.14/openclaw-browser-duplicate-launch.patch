diff --git a/extensions/browser/src/browser/server-context.availability.ts b/extensions/browser/src/browser/server-context.availability.ts
index 531e625..2b574c9 100644
--- a/extensions/browser/src/browser/server-context.availability.ts
+++ b/extensions/browser/src/browser/server-context.availability.ts
@@ -1,4 +1,5 @@
 import fs from "node:fs";
+import { createSubsystemLogger } from "../logging/subsystem.js";
 import { resolveCdpReachabilityPolicy } from "./cdp-reachability-policy.js";
 import {
   CHROME_MCP_ATTACH_READY_POLL_MS,
@@ -52,6 +53,8 @@ type AvailabilityOps = {
   stopRunningBrowser: () => Promise<{ stopped: boolean }>;
 };

+const log = createSubsystemLogger("browser").child("availability");
+
 export function createProfileAvailability({
   opts,
   profile,
@@ -171,7 +174,7 @@ export function createProfileAvailability({
     );
   };

-  const ensureBrowserAvailable = async (): Promise<void> => {
+  const doEnsureBrowserAvailable = async (): Promise<void> => {
     await reconcileProfileRuntime();
     if (capabilities.usesChromeMcp) {
       if (profile.userDataDir && !fs.existsSync(profile.userDataDir)) {
@@ -199,11 +202,14 @@ export function createProfileAvailability({
       // Browser control service can restart while a loopback OpenClaw browser is still
       // alive. Give that pre-existing browser one longer probe window before falling
       // back to local executable resolution.
-      if (!attachOnly && !remoteCdp && profile.cdpIsLoopback && !profileState.running) {
+      if (!attachOnly && !remoteCdp && profile.cdpIsLoopback) {
         if (
           (await isHttpReachable(PROFILE_ATTACH_RETRY_TIMEOUT_MS)) &&
           (await isReachable(PROFILE_ATTACH_RETRY_TIMEOUT_MS))
         ) {
+          log.info(
+            `reused managed browser profile "${profile.name}" after CDP reachability retry`,
+          );
           return;
         }
       }
@@ -214,6 +220,9 @@ export function createProfileAvailability({
             : `Browser attachOnly is enabled and profile "${profile.name}" is not running.`,
         );
       }
+      log.warn(
+        `launching managed browser profile "${profile.name}" because CDP remained unreachable`,
+      );
       const launched = await launchOpenClawChrome(current.resolved, profile);
       attachRunning(launched);
       try {
@@ -231,6 +240,15 @@ export function createProfileAvailability({
       return;
     }

+    if (!attachOnly && !remoteCdp && profile.cdpIsLoopback) {
+      if (await isReachable(PROFILE_ATTACH_RETRY_TIMEOUT_MS)) {
+        log.info(
+          `reused managed browser profile "${profile.name}" after CDP websocket retry`,
+        );
+        return;
+      }
+    }
+
     // HTTP responds but WebSocket fails. For attachOnly/remote profiles, never perform
     // local ownership/restart handling; just run attach retries and surface attach errors.
     if (attachOnly || remoteCdp) {
@@ -258,6 +276,9 @@ export function createProfileAvailability({
       );
     }

+    log.warn(
+      `restarting managed browser profile "${profile.name}" because CDP websocket is unreachable`,
+    );
     await stopOpenClawChrome(profileState.running);
     setProfileRunning(null);

@@ -271,6 +292,24 @@ export function createProfileAvailability({
     }
   };

+  const ensureBrowserAvailable = async (): Promise<void> => {
+    const profileState = getProfileState();
+    if (profileState.ensureInFlight) {
+      await profileState.ensureInFlight;
+      return;
+    }
+
+    let ensurePromise: Promise<void>;
+    ensurePromise = doEnsureBrowserAvailable().finally(() => {
+      const currentProfileState = getProfileState();
+      if (currentProfileState.ensureInFlight === ensurePromise) {
+        currentProfileState.ensureInFlight = null;
+      }
+    });
+    profileState.ensureInFlight = ensurePromise;
+    await ensurePromise;
+  };
+
   const stopRunningBrowser = async (): Promise<{ stopped: boolean }> => {
     await reconcileProfileRuntime();
     if (capabilities.usesChromeMcp) {
diff --git a/extensions/browser/src/browser/server-context.ensure-browser-available.waits-for-cdp-ready.test.ts b/extensions/browser/src/browser/server-context.ensure-browser-available.waits-for-cdp-ready.test.ts
index 39f3e57..45177a0 100644
--- a/extensions/browser/src/browser/server-context.ensure-browser-available.waits-for-cdp-ready.test.ts
+++ b/extensions/browser/src/browser/server-context.ensure-browser-available.waits-for-cdp-ready.test.ts
@@ -1,3 +1,5 @@
+import type { ChildProcessWithoutNullStreams } from "node:child_process";
+import { EventEmitter } from "node:events";
 import { afterEach, describe, expect, it, vi } from "vitest";
 import "./server-context.chrome-test-harness.js";
 import {
@@ -88,6 +90,36 @@ describe("browser server-context ensureBrowserAvailable", () => {
     expect(stopOpenClawChrome).not.toHaveBeenCalled();
   });

+  it("reuses a tracked loopback browser after an initial short probe miss", async () => {
+    const { launchOpenClawChrome, stopOpenClawChrome, isChromeCdpReady, profile, state } =
+      setupEnsureBrowserAvailableHarness();
+    const isChromeReachable = vi.mocked(chromeModule.isChromeReachable);
+    state.resolved.ssrfPolicy = {};
+    state.profiles.set("openclaw", {
+      profile: profile.profile,
+      running: {
+        pid: 456,
+        exe: { kind: "chromium", path: "/usr/bin/chromium" },
+        userDataDir: "/tmp/openclaw-test",
+        cdpPort: 18800,
+        startedAt: Date.now(),
+        proc: new EventEmitter() as ChildProcessWithoutNullStreams,
+      },
+      lastTargetId: null,
+      reconcile: null,
+    });
+
+    isChromeReachable.mockResolvedValueOnce(false).mockResolvedValueOnce(true);
+    isChromeCdpReady.mockResolvedValueOnce(true);
+
+    await expect(profile.ensureBrowserAvailable()).resolves.toBeUndefined();
+
+    expect(isChromeReachable).toHaveBeenCalledTimes(2);
+    expect(isChromeCdpReady).toHaveBeenCalledTimes(1);
+    expect(launchOpenClawChrome).not.toHaveBeenCalled();
+    expect(stopOpenClawChrome).not.toHaveBeenCalled();
+  });
+
   it("retries remote CDP websocket reachability once before failing", async () => {
     const { launchOpenClawChrome, stopOpenClawChrome, isChromeCdpReady } =
       setupEnsureBrowserAvailableHarness();
@@ -132,6 +164,22 @@ describe("browser server-context ensureBrowserAvailable", () => {
     expect(stopOpenClawChrome).not.toHaveBeenCalled();
   });

+  it("serializes concurrent managed browser launch checks for one profile", async () => {
+    const { launchOpenClawChrome, stopOpenClawChrome, isChromeCdpReady, profile } =
+      setupEnsureBrowserAvailableHarness();
+    const isChromeReachable = vi.mocked(chromeModule.isChromeReachable);
+    isChromeReachable.mockResolvedValue(false);
+    isChromeCdpReady.mockResolvedValue(true);
+    mockLaunchedChrome(launchOpenClawChrome, 654);
+
+    await expect(
+      Promise.all([profile.ensureBrowserAvailable(), profile.ensureBrowserAvailable()]),
+    ).resolves.toEqual([undefined, undefined]);
+
+    expect(launchOpenClawChrome).toHaveBeenCalledTimes(1);
+    expect(stopOpenClawChrome).not.toHaveBeenCalled();
+  });
+
   it("treats attachOnly loopback CDP as local control with remote-class probe timeouts", async () => {
     const { launchOpenClawChrome, stopOpenClawChrome } = setupEnsureBrowserAvailableHarness();
     const isChromeReachable = vi.mocked(chromeModule.isChromeReachable);
diff --git a/extensions/browser/src/browser/server-context.types.ts b/extensions/browser/src/browser/server-context.types.ts
index 00a813f..64ff485 100644
--- a/extensions/browser/src/browser/server-context.types.ts
+++ b/extensions/browser/src/browser/server-context.types.ts
@@ -11,6 +11,8 @@ export type { BrowserTab };
 export type ProfileRuntimeState = {
   profile: ResolvedBrowserProfile;
   running: RunningChrome | null;
+  /** In-flight availability check so concurrent browser actions cannot launch twice. */
+  ensureInFlight?: Promise<void> | null;
   /** Sticky tab selection when callers omit targetId (keeps snapshot+act consistent). */
   lastTargetId?: string | null;
   reconcile?: {
