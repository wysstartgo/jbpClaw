diff --git a/extensions/browser/src/browser/config.test.ts b/extensions/browser/src/browser/config.test.ts
index 80aa0ba000..01e4fd1000 100644
--- a/extensions/browser/src/browser/config.test.ts
+++ b/extensions/browser/src/browser/config.test.ts
@@ -297,12 +297,14 @@ describe("browser config", () => {
       ssrfPolicy: {
         allowPrivateNetwork: true,
         allowedHostnames: [" localhost ", ""],
         hostnameAllowlist: [" *.trusted.example ", " "],
+        blockedHostnames: [" www.baidu.com ", ""],
       },
     } as unknown as BrowserConfig);
     expect(resolved.ssrfPolicy).toEqual({
       dangerouslyAllowPrivateNetwork: true,
       allowedHostnames: ["localhost"],
       hostnameAllowlist: ["*.trusted.example"],
+      blockedHostnames: ["www.baidu.com"],
     });
   });
 
diff --git a/extensions/browser/src/browser/config.ts b/extensions/browser/src/browser/config.ts
index 02787c2000..08ee7c4000 100644
--- a/extensions/browser/src/browser/config.ts
+++ b/extensions/browser/src/browser/config.ts
@@ -132,6 +132,7 @@ function resolveBrowserSsrFPolicy(cfg: BrowserConfig | undefined): SsrFPolicy |
   const dangerouslyAllowPrivateNetwork = rawPolicy?.dangerouslyAllowPrivateNetwork;
   const allowedHostnames = normalizeStringList(rawPolicy?.allowedHostnames);
   const hostnameAllowlist = normalizeStringList(rawPolicy?.hostnameAllowlist);
+  const blockedHostnames = normalizeStringList(rawPolicy?.blockedHostnames);
   const hasExplicitPrivateSetting =
     allowPrivateNetwork !== undefined || dangerouslyAllowPrivateNetwork !== undefined;
   const resolvedAllowPrivateNetwork =
@@ -141,7 +142,8 @@ function resolveBrowserSsrFPolicy(cfg: BrowserConfig | undefined): SsrFPolicy |
     !resolvedAllowPrivateNetwork &&
     !hasExplicitPrivateSetting &&
     !allowedHostnames &&
-    !hostnameAllowlist
+    !hostnameAllowlist &&
+    !blockedHostnames
   ) {
     // Keep the default policy object present so CDP guards still enforce
     // fail-closed private-network checks on unconfigured installs.
@@ -156,6 +158,7 @@ function resolveBrowserSsrFPolicy(cfg: BrowserConfig | undefined): SsrFPolicy |
       : {}),
     ...(allowedHostnames ? { allowedHostnames } : {}),
     ...(hostnameAllowlist ? { hostnameAllowlist } : {}),
+    ...(blockedHostnames ? { blockedHostnames } : {}),
   };
 }
 
diff --git a/src/config/types.browser.ts b/src/config/types.browser.ts
index 66c030f000..26a0c06000 100644
--- a/src/config/types.browser.ts
+++ b/src/config/types.browser.ts
@@ -28,6 +28,11 @@ export type BrowserSsrFPolicyConfig = {
    * Supports exact hosts and "*.example.com" wildcard subdomains.
    */
   hostnameAllowlist?: string[];
+  /**
+   * Hostnames blocked for browser navigation.
+   * Supports exact hosts and "*.example.com" wildcard subdomains.
+   */
+  blockedHostnames?: string[];
 };
 export type BrowserConfig = {
   enabled?: boolean;
diff --git a/src/config/zod-schema.ts b/src/config/zod-schema.ts
index 1724b9f000..65c2c9b000 100644
--- a/src/config/zod-schema.ts
+++ b/src/config/zod-schema.ts
@@ -372,6 +372,7 @@ export const OpenClawConfigSchema = z
             dangerouslyAllowPrivateNetwork: z.boolean().optional(),
             allowedHostnames: z.array(z.string()).optional(),
             hostnameAllowlist: z.array(z.string()).optional(),
+            blockedHostnames: z.array(z.string()).optional(),
           })
           .strict()
           .optional(),
diff --git a/src/infra/net/ssrf.pinning.test.ts b/src/infra/net/ssrf.pinning.test.ts
index 8d26c0f000..cb09662000 100644
--- a/src/infra/net/ssrf.pinning.test.ts
+++ b/src/infra/net/ssrf.pinning.test.ts
@@ -140,6 +140,18 @@ describe("SSRF hostname pinning", () => {
       }),
     ).rejects.toThrow(/allowlist/i);
   });
+
+  it("blocks configured hostnames before DNS lookup", async () => {
+    const lookup = createPublicLookupMock();
+
+    await expect(
+      resolvePinnedHostnameWithPolicy("www.baidu.com", {
+        lookupFn: lookup,
+        policy: { blockedHostnames: ["www.baidu.com"] },
+      }),
+    ).rejects.toThrow(/blocked hostname/i);
+    expect(lookup).not.toHaveBeenCalled();
+  });
 
   it.each([
     {
diff --git a/src/infra/net/ssrf.ts b/src/infra/net/ssrf.ts
index c632e30000..d19516e000 100644
--- a/src/infra/net/ssrf.ts
+++ b/src/infra/net/ssrf.ts
@@ -42,6 +42,7 @@ export type SsrFPolicy = {
   allowRfc2544BenchmarkRange?: boolean;
   allowedHostnames?: string[];
   hostnameAllowlist?: string[];
+  blockedHostnames?: string[];
 };
 
 const BLOCKED_HOSTNAMES = new Set([
@@ -81,6 +82,11 @@ function shouldSkipPrivateNetworkChecks(hostname: string, policy?: SsrFPolicy):
   );
 }
 
+function isHostnameBlockedByPolicy(hostname: string, policy?: SsrFPolicy): boolean {
+  const blockedHostnames = normalizeHostnameAllowlist(policy?.blockedHostnames);
+  return blockedHostnames.length > 0 && matchesHostnameAllowlist(hostname, blockedHostnames);
+}
+
 function resolveIpv4SpecialUseBlockOptions(policy?: SsrFPolicy): Ipv4SpecialUseBlockOptions {
   return {
     allowRfc2544BenchmarkRange: policy?.allowRfc2544BenchmarkRange === true,
@@ -205,6 +211,10 @@ function resolveHostnamePolicyChecks(
   const hostnameAllowlist = normalizeHostnameAllowlist(policy?.hostnameAllowlist);
   const skipPrivateNetworkChecks = shouldSkipPrivateNetworkChecks(normalized, policy);
 
+  if (isHostnameBlockedByPolicy(normalized, policy)) {
+    throw new SsrFBlockedError(`Blocked hostname (configured blocklist): ${hostname}`);
+  }
+
   if (!matchesHostnameAllowlist(normalized, hostnameAllowlist)) {
     throw new SsrFBlockedError(`Blocked hostname (not in allowlist): ${hostname}`);
   }
